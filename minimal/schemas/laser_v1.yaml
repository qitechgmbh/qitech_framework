qms_version: 1.0
revision: 1

identification:
  name: laser_v1
  vendor_id: 1
  machine_id: 6

config:
  diameter:
    target: !millimeter
    target_default: !millimeter
    target_enabled: !boolean
    target_min: !millimeter
    target_max: !millimeter

    tolerance:
      upper: !millimeter
      lower: !millimeter

  subscribed:
    diameter:
      target: !millimeter
        nullable: true

      tolerance.lower: !millimeter
        nullable: true

      tolerance.upper: !millimeter
        nullable: true

state:
  in_tolerance: !boolean

  # subscribed test
  subscribed.in_tolerance: !boolean
      nullable: true

measurements:
  diameter: !millimeter
  diameter_x: !millimeter
  diameter_y: !millimeter
  roundness: !millimeter

  # subscribed test
  subscribed.diameter: !millimeter
    nullable: true

  subscribed.diameter_x: !millimeter
    nullable: true

  subscribed.diameter_y: !millimeter
    nullable: true

  subscribed.roundness: !millimeter
    nullable: true