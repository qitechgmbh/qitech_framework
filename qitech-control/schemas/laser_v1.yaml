qms_version: 1.0

name: laser_v1
schema_revision: 1

identification:
  vendor: 1
  machine: 6

config:
  diameter:
    target: !millimeter
      default: 1.75
      range: 0..

    tolerance:
      upper: !millimeter
        default: 0.05
        range: 0..

      lower: !millimeter
        default: 0.05
        range: 0..

state:
  in_tolerance: !boolean

measurements:
  diameter: !millimeter
  diameter_x: !millimeter
  diameter_y: !millimeter
  roundness: !millimeter
