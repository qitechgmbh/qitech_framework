[package]
name = "qitech_framework_macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# --- internal deps ---
qitech_framework_common = { path = "../common" }
syn = { version = "3.0.2" , features = ["full", "visit-mut"]}
quote = "1"
proc-macro2 = "1"
