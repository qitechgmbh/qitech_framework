[package]
name = "control_macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# --- internal deps ---
control_core = { path = "../control-core" }
syn = { version = "3.0.2" , features = ["full", "visit-mut"]}
quote = "1"
proc-macro2 = "1"
