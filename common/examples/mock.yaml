qms_version: 1.0
revision: 1

identification:
  name: mock_machine
  vendor_id: 1
  machine_id: 99

config: 
  hello: !enum
    variants: [apple, tomato, carrot]
    default: apple
    nullable: true

  world: !enum
    variants: [apple, tomato, carrot]
    default: apple

  num: !millimeter
    default: 1.0
    nullable: true

state:
  enum: !enum 
    variants: [apple, tomato, carrot]

measurements:
  counter: !float

commands:
  start_winding: !command

  puller:
    start: !command
      arg0: !float
      arg1: !enum
        variants: [apple, tomato, carrot]

    group:
      arg_2: !command


events: 
  hello: !event
    world: !string
    object: !object
      fields: 
        hello: !string

  event_v2: !event
    cd: !millimeter
      
metadata:
  config:
    hello:  
      description:
        de-DE: "Meine Enum"

      eks_dee: ""

  commands:
    puller:
      start:
        description:
          de-DE: "WHAT"